fragment NodesParts on Query {
  findAllNodes {
    createTime
    id
    ipInterfaces {
      id
      ipAddress
      nodeId
      snmpPrimary
    }
    location {
      id
      location
    }
    monitoringLocationId
    nodeLabel
    scanType
  }
}
