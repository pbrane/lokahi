---
apiVersion: v1
kind: Service
metadata:
  name: flows-ingester
  labels:
    app: flows-ingester
spec:
  type: ClusterIP
  ports:
    - port: 8282
      protocol: TCP
      name: http
    - port: 9000
      protocol: TCP
      name: grpc
  selector:
    app: flows-ingester
