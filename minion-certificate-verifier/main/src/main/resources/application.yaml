spring:
  application:
    name: lokahi-minion-certificate-verifier

grpc:
  server:
    deadline: 6000
  url:
    inventory: opennms-inventory:6565

management:
  endpoints:
    web:
      exposure:
        include: "*"
    health:
      probes:
        enabled: true
