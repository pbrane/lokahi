apiVersion: v1
kind: Secret
metadata:
  name: minion-gateway-tls
  labels:
    app: {{ .Values.OpenNMS.MinionGateway.ServiceName }}
  namespace: {{ .Release.Namespace }}
data:
  tls.crt: "{{ .Files.Get "tls/tls.crt" | b64enc }}"
  tls.key: "{{ .Files.Get "tls/tls.key" | b64enc }}"
type: Opaque