apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.OpenNMS.Email.ServiceName }}
  labels:
    app: {{ .Values.OpenNMS.Email.ServiceName }}
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.OpenNMS.Email.GrpcPort }}
      protocol: TCP
      name: grpc
  selector:
    app: {{ .Values.OpenNMS.Email.ServiceName }}
