{{- if (eq (include "onOpenShift" .) "false") }}
{{- if .Values.Prometheus.Enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.Prometheus.ServiceName }}-sa1
  labels:
    app: {{ .Values.Prometheus.ServiceName }}
{{- end }}
{{- end }}