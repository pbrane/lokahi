
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.3.0
  creationTimestamp: null
  name: opennms.k8s.opennms.com
spec:
  group: k8s.opennms.com
  names:
    kind: OpenNMS
    listKind: OpenNMSList
    plural: opennms
    shortNames:
    - onms
    singular: opennms
  scope: Namespaced
  subresources:
    status: {}
  validation:
    openAPIV3Schema:
      description: OpenNMS - is the Schema for the opennms API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: OpenNMSSpec defines the desired state of OpenNMS
          properties:
            api:
              description: Defines service values for API service
              properties:
                cpu:
                  type: string
                disk:
                  type: string
                image:
                  description: Image tag version of OpenNMS.
                  type: string
                mem:
                  type: string
              type: object
            core:
              description: Defines service values for Core service
              properties:
                cpu:
                  type: string
                disk:
                  type: string
                image:
                  description: Image tag version of OpenNMS.
                  type: string
                mem:
                  type: string
              type: object
            credentials:
              description: Set the default credentials for the instance
              properties:
                adminPassword:
                  type: string
                userPassword:
                  type: string
              type: object
            deployOnly:
              default: false
              description: Only deploy the instance, do not run recurring updates
                on it
              type: boolean
            grafana:
              description: Defines service values for Grafana
              properties:
                cpu:
                  type: string
                disk:
                  type: string
                image:
                  description: Image tag version of OpenNMS.
                  type: string
                mem:
                  type: string
              type: object
            host:
              description: Domain name used in ingress rule
              type: string
            httpPort:
              default: 80
              description: HTTP port number the instance will be exposed on
              type: integer
            httpsPort:
              default: 443
              description: HTTPS port number the instance will be exposed on
              type: integer
            imageUpdate:
              description: Defines the logic of ONMS image update
              properties:
                update:
                  description: can have values of now/none
                  type: string
              type: object
            inventory:
              description: Defines service values for Inventory service
              properties:
                cpu:
                  type: string
                disk:
                  type: string
                image:
                  description: Image tag version of OpenNMS.
                  type: string
                mem:
                  type: string
              type: object
            keycloak:
              description: Defines service values for Postgres
              properties:
                cpu:
                  type: string
                disk:
                  type: string
                image:
                  description: Image tag version of OpenNMS.
                  type: string
                mem:
                  type: string
              type: object
            metricsProcessor:
              description: Defines service values for MetricsProcessor service
              properties:
                cpu:
                  type: string
                disk:
                  type: string
                image:
                  description: Image tag version of OpenNMS.
                  type: string
                mem:
                  type: string
              type: object
            minion:
              description: Defines service values for the Minion
              properties:
                cpu:
                  type: string
                disk:
                  type: string
                image:
                  description: Image tag version of OpenNMS.
                  type: string
                mem:
                  type: string
              type: object
            minionGateway:
              description: Defines service values for the Minion Gateway
              properties:
                cpu:
                  type: string
                disk:
                  type: string
                image:
                  description: Image tag version of OpenNMS.
                  type: string
                mem:
                  type: string
              type: object
            namespace:
              description: K8s namespace to use
              type: string
            notification:
              description: Defines service values for Notification service
              properties:
                cpu:
                  type: string
                disk:
                  type: string
                image:
                  description: Image tag version of OpenNMS.
                  type: string
                mem:
                  type: string
              type: object
            postgres:
              description: Defines service values for Postgres
              properties:
                cpu:
                  type: string
                disk:
                  type: string
                image:
                  description: Image tag version of OpenNMS.
                  type: string
                mem:
                  type: string
              type: object
            ui:
              description: Defines service values for UI service
              properties:
                cpu:
                  type: string
                disk:
                  type: string
                image:
                  description: Image tag version of OpenNMS.
                  type: string
                mem:
                  type: string
              type: object
          required:
          - namespace
          type: object
        status:
          description: OpenNMSStatus - defines the observed state of OpenNMS
          properties:
            image:
              description: ImageStatus - defines current status of used image for
                OpenNMS container
              properties:
                checkedAt:
                  description: timestamp of a last image check in DockerHub
                  type: string
                isLatest:
                  description: true if latest image used, false otherwise
                  type: boolean
                servicesToUpdate:
                  description: list of services that have updates available
                  type: string
              required:
              - isLatest
              type: object
            nodes:
              items:
                type: string
              type: array
            readiness:
              description: ReadinessStatus - the ready status of the ONMS instance
              properties:
                ready:
                  description: if the ONMS instance is ready
                  type: boolean
                reason:
                  description: reason an ONMS instance isn't ready
                  type: string
                services:
                  description: list of readinesses of the constituent services
                  items:
                    properties:
                      ready:
                        description: if the service is ready
                        type: boolean
                      reason:
                        description: reason a service isn't ready
                        type: string
                      timestamp:
                        description: the time the `ready` flag was last updated
                        type: string
                    required:
                    - ready
                    - reason
                    type: object
                  type: array
                timestamp:
                  description: the time the `ready` flag was last updated
                  type: string
              type: object
          type: object
      type: object
  version: v1alpha1
  versions:
  - name: v1alpha1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
